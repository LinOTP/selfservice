# Depends: dir
#
# LinOTP self service NG interface
# The interface is reachable at <linotp-url>/selfservice-v2

<IfModule mod_dir.c>

  Alias /selfservice-v2 /usr/share/linotp-selfservice/en

  <Directory /usr/share/linotp-selfservice/en>
    FallbackResource /selfservice-v2/index.html
  </Directory>

</IfModule>
