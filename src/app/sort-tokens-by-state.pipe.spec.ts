import { SortTokensByStatePipe } from './sort-tokens-by-state.pipe';

describe('SortTokensByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortTokensByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
